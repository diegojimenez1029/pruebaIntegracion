
{
  "name": "ci_pipeline_project",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "test": "jest --coverage"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
